<div
    id="navbar"
    style="z-index: 999; background:#3B3E99"
    class="w-full pt-2 md:pt-4 absolute top-0 grid grid-cols-1 sm:w-full sm:grid-cols-7 gap-1 md:gap-2 lg:text-2xl text-center"
>
    <a
        href="#início"
        class="w-full flex flex-col items-center cursor-pointer"
    >
        <div class="bg-white rounded p-2 w-1/2 sm:w-full shadow drop-shadow">
            <img 
            src="media/component/navbar/logo.webp"
            alt="logo dedicar equipe"
            loading="lazy"
             />
        </div>
    </a>
    {
        [
            "serviço",
            "diferencial",
            "sobre",
            "depoimentos",
            "parceiros",
            "contato",
        ].map((item, index) => {
            return (
                <a
                    href={`#${item}`}
                    class={`hidden sm:flex text-base font-semibold tracking-wider  items-center justify-center rounded bg-[#3B3E99] hover:bg-[#00B0F2] hover:text-[#3B3E99] hover:border-[#3B3E99] hover:shadow hover:drop-shadow hover:font-black cursor-pointer ${
                        index == 4 ? "col-span-2" : ""
                    } sm:col-span-1`}
                >
                    {item}
                </a>
            );
        })
    }
</div>
<style>

</style>
